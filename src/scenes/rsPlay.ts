import Phaser = require('phaser');

export class RsPlayScene extends Phaser.Scene {
    constructor() {
        super({
            key: "RsPlayScene"
        });
    }

    preload(): void {
    }

    init(): void {
    }

    create(): void {
    }

	update(): void {
	}
}
