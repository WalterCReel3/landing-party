import Phaser = require('phaser');

export class PursuerMoveScene extends Phaser.Scene {
    constructor() {
        super({
            key: "PursuerMoveScene"
        });
    }

    preload(): void {
    }

    init(): void {
    }

    create(): void {
    }

	update(): void {
	}
}
