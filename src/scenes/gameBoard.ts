import Phaser = require('phaser');

export class GameBoardScene extends Phaser.Scene {
    constructor() {
        super({
            key: "GameBoardScene"
        });
    }

    preload(): void {
    }

    init(): void {
    }

    create(): void {
    }

	update(): void {
	}
}
