import Phaser = require('phaser');

export class LevelOneScene extends Phaser.Scene {
    constructor() {
        super({
            key: "LevelOneScene"
        });
    }

    preload(): void {
    }

    init(): void {
    }

    create(): void {
    }

	update(): void {
	}
}
